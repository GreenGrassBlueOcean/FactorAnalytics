% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-risk.R
\name{normalize_fm_residuals}
\alias{normalize_fm_residuals}
\title{Normalize factor model residuals to unit-variance pseudo-factors}
\usage{
normalize_fm_residuals(resid_mat, resid_sd, weights = NULL)
}
\arguments{
\item{resid_mat}{T x N matrix of residuals (typically from residuals(object))}

\item{resid_sd}{N-vector of residual standard deviations}

\item{weights}{optional N-vector of portfolio weights for aggregation}
}
\value{
xts object: T x N (asset-level) or T x 1 (portfolio-level)
}
\description{
Divides each asset's residuals by its residual standard deviation to
produce standardized residuals z(t) = e(t) / sigma_e with approximately
unit variance. Optionally aggregates to portfolio level:
  z_p(t) = sum(w_i * e_it) / sqrt(sum(w_i^2 * sigma_i^2))
}
\keyword{internal}
